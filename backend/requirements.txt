fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
pydantic==2.10.4
pydantic-settings==2.7.0
python-multipart==0.0.20
python-dotenv==1.0.1
pypdf==5.1.0
python-docx==1.1.2
openai==1.58.1
httpx==0.28.1
aiofiles==24.1.0
pytest==8.3.4
pytest-asyncio==0.25.0

# Optional extras (not installed by default):
#   chromadb  -> enables VECTOR_STORE=chroma backend (adds a large dep tree)
# The default embedding provider (`mock`) and vector store (`local`) have no
# additional dependencies; no PyTorch is required at any point.

